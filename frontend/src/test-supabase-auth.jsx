// Test file to verify Supabase authentication
// Replace the content of main.jsx with this temporarily for testing

import React from 'react'
import ReactDOM from 'react-dom/client'
import SupabaseApp from './SupabaseApp.jsx'
import './i18n/index.js' // Initialize i18n
import './styles/App.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SupabaseApp />
  </React.StrictMode>,
)